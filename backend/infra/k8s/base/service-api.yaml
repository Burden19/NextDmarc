apiVersion: v1
kind: Service
metadata:
  name: nextdmarc-api
  namespace: nextdmarc
spec:
  selector:
    app: nextdmarc-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
